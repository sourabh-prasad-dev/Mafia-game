# Copy this file to .env.local for local development (no changes needed)
# For production, set these as environment variables in Vercel dashboard

# Leave empty for local dev (Vite proxy handles it)
# VITE_API_URL=
# VITE_BACKEND_URL=

# Example production values:
VITE_API_URL=https://mafia-backend.up.railway.app
VITE_BACKEND_URL=https://mafia-backend.up.railway.app
